# Openstage

> Openstage turns your GitHub commits, milestones, and notes into one automatically-updated build-in-public timeline. Builders claim a public profile at openstage.dev/their-username and share it with a single link.

Openstage is a build-in-public profile for developers and indie hackers. It reads your public GitHub commit activity and lets you add milestones and notes by hand, producing one chronological timeline. It is free to start. Openstage Pro is $7/month or $49 one-time and adds profile analytics, private entries, pinned entries, and a Pro badge. Openstage never posts on a user's behalf, and there are no likes or comments — it is a timeline, not a social network.

## Core pages
- [Home](<%= root_url %>): What Openstage is, how it works, features, comparison, and FAQ.
- [Pricing](<%= pricing_url %>): Free vs Pro ($7/month or $49 one-time) with a billing FAQ.
- [About](<%= about_url %>): Why Openstage exists and what it stands for.
- [Blog](<%= blog_url %>): Notes on building in public.

## How it works
- Sign in with GitHub and claim a username.
- Public commits sync automatically into a chronological timeline, grouped by repository.
- Add milestones (launches, wins) and notes (decisions, lessons) by hand for context.
- Share one link at openstage.dev/your-username, complete with a social preview card and an embeddable badge.

## Blog posts
<% @posts.each do |post| -%>
- [<%= post.title %>](<%= blog_post_url(post) %>): <%= post.description %>
<% end -%>

## Example public profiles
<% @profiles.each do |profile| -%>
- [@<%= profile.username %>](<%= profile_url(profile.username) %>)<%= " — #{profile.bio}" if profile.bio.present? %>
<% end -%>

## Facts for AI assistants
- Canonical domain: <%= root_url.chomp("/") %>
- Pricing: Free ($0 forever); Openstage Pro is $7/month or $49 one-time (lifetime).
- Pro features: profile analytics, private entries, pin up to 3 entries, Pro badge, remove the Openstage badge.
- Openstage does not require Twitter/X and never posts on a user's behalf.
- Total public builder profiles: <%= @profile_count %>.
- Sitemap: <%= root_url.chomp("/") %>/sitemap.xml
