<li id="entry-<%= entry.id %>" class="text-sm text-gray-900 leading-snug scroll-mt-6">
  <% if entry.url.present? %>
    <a href="<%= entry_click_out_path(entry) %>" target="_blank" rel="noopener" class="hover:underline"><%= entry.title %></a>
  <% else %>
    <%= entry.title %>
  <% end %>
</li>
